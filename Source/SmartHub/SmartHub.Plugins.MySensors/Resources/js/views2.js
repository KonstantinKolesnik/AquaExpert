
define(
	['app', 'marionette', 'backbone', 'underscore', 'text!webapp/mysensors/templates2.html'],
    function (application, marionette, backbone, _, templates) {

        return {
            //layoutView: mLayout,
            //nodesView: mNodesView
        };
    });