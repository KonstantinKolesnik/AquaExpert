
@using SmartHub.Dashboard.Common;
@model AppSectionItem

@{
    ViewData["Title"] = Model.Name;

    var htmlDto = await Utils.GETRequest(ViewBag.ServerUrl + Model.Url);
    var html = Utils.DtoDeserialize<string>(htmlDto);
}

<div>@Html.Raw(html)</div>




@*<div>@{Html.RenderPartial("PartialViews/pvWemos");}</div>*@
